#include<iostream>
using namespace std;

int main()
{
    int a[100], n, i, key;
    int low=0, high, mid;

    cin>>n;
    for(i=0;i<n;i++)
        cin>>a[i];

    cin>>key;

    high = n-1;

    while(low <= high)
    {
        mid = (low + high) / 2;

        if(a[mid] == key)
        {
            cout<<"Found at position "<<mid+1;
            return 0;
        }
        else if(a[mid] < key)
            low = mid + 1;
        else
            high = mid - 1;
    }

    cout<<"Not Found";

    return 0;
}
